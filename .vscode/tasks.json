{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "go-prelaunchtasks",
            "type": "shell",
            "command": "go mod tidy && go fmt ./... && golangci-lint run",
            "group": "build",
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}